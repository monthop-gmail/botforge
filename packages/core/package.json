{
  "name": "@botforge/core",
  "version": "0.0.1",
  "type": "module",
  "license": "MIT",
  "description": "Channel/session/context ที่ไม่ผูกกับ runtime — ของที่เคยถูก copy อยู่ใน 23 ไฟล์",
  "exports": {
    ".": "./src/index.ts",
    "./identity": "./src/identity.ts",
    "./errors": "./src/errors.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.7.0"
  }
}
